---
title: "OpenTelemetry Integration"
description: "Traces, Metriken und Logs mit dem OpenTelemetry SDK für SSID Infrastructure"
---

## Übersicht

SSID nutzt [OpenTelemetry](https://opentelemetry.io/) als einheitliches Observability Framework für Traces, Metriken und Structured Logs über alle Services.

## Traces

Distributed Tracing bietet End-to-End Visibility über Gate Chains, API Calls und Agent Dispatches.

### Python (FastAPI Backend)

```python
from opentelemetry import trace
from opentelemetry.sdk.trace import TracerProvider
from opentelemetry.sdk.trace.export import BatchSpanProcessor
from opentelemetry.exporter.otlp.proto.grpc.trace_exporter import OTLPSpanExporter

provider = TracerProvider()
processor = BatchSpanProcessor(OTLPSpanExporter(endpoint="http://localhost:4317"))
provider.add_span_processor(processor)
trace.set_tracer_provider(provider)

tracer = trace.get_tracer("ssid-ems")

@app.middleware("http")
async def trace_requests(request, call_next):
    with tracer.start_as_current_span(
        f"{request.method} {request.url.path}",
        attributes={"http.method": request.method, "http.url": str(request.url)},
    ):
        response = await call_next(request)
        return response
```

### TypeScript (Express Server)

```typescript
import { NodeTracerProvider } from '@opentelemetry/sdk-trace-node';
import { OTLPTraceExporter } from '@opentelemetry/exporter-trace-otlp-grpc';
import { BatchSpanProcessor } from '@opentelemetry/sdk-trace-base';

const provider = new NodeTracerProvider();
provider.addSpanProcessor(new BatchSpanProcessor(new OTLPTraceExporter()));
provider.register();
```

## Metriken

Prometheus-kompatible Metriken für Gate Performance, Evidence Integrität und Resource Utilization.

```python
from opentelemetry import metrics
from opentelemetry.sdk.metrics import MeterProvider
from opentelemetry.exporter.prometheus import PrometheusMetricReader

reader = PrometheusMetricReader()
provider = MeterProvider(metric_readers=[reader])
metrics.set_meter_provider(provider)

meter = metrics.get_meter("ssid-ems")
gate_duration = meter.create_histogram("gate.duration_ms", description="Gate execution time in ms")
evidence_writes = meter.create_counter("evidence.writes_total", description="Total evidence writes")
```

## Logs

Structured Logging mit Grafana Loki via OpenTelemetry Log Bridge.

```python
import logging
from opentelemetry._logs import set_logger_provider
from opentelemetry.sdk._logs import LoggerProvider
from opentelemetry.sdk._logs.export import BatchLogRecordProcessor
from opentelemetry.exporter.otlp.proto.grpc._log_exporter import OTLPLogExporter

logger_provider = LoggerProvider()
logger_provider.add_log_record_processor(BatchLogRecordProcessor(OTLPLogExporter()))
set_logger_provider(logger_provider)
```

## Konfiguration

Umgebungsvariablen für OTEL Collector:

| Variable | Default | Beschreibung |
|----------|---------|-------------|
| `OTEL_EXPORTER_OTLP_ENDPOINT` | `http://localhost:4317` | Collector gRPC Endpoint |
| `OTEL_SERVICE_NAME` | `ssid-ems` | Service Identifier |
| `OTEL_TRACES_SAMPLER` | `parentbased_traceidratio` | Sampling Strategy |
| `OTEL_TRACES_SAMPLER_ARG` | `0.1` | Sampling Ratio (10%) |
